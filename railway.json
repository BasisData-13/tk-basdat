{
    "$schema": "https://schema.up.railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "nixpacksPlan": {
        "providers": ["python"],
        "phases": {
          "setup": {
            "nixPkgs": ["...", "nodejs"]
          },
          "npm:install": {
            "dependsOn": ["setup"],
            "cmds": ["npm ci"]
          }
        }
      }
    },
    "deploy": {
      "startCommand": "python -m pip install django-tailwind && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn marmut.wsgi && python manage.py tailwind install && python manage.py tailwind start && python manage.py runserver",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
}